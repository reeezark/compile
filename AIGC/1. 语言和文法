下面这部分是编译原理里非常核心的基础：**语言和文法**。你可以先记住一句话：

> **语言是“所有合法句子的集合”，文法是“描述这些合法句子怎么生成的规则”。**

在编译原理中，源代码本质上就是一种语言，比如 C、Java、Go、Python。编译器要做的第一件事，就是判断你写的代码是不是符合这门语言的规则。

------

# 一、先从直觉理解：语言是什么？

我们平时说的中文、英文是自然语言。

编译原理里说的语言，通常是**形式语言**，也就是用严格数学规则定义出来的语言。

比如：

```text
1 + 2 * 3
```

这是一个合法的算术表达式。

但是：

```text
+ * 1 2
```

一般就不是一个合法的算术表达式。

所以我们可以说：

> 算术表达式语言，就是所有合法算术表达式组成的集合。

再比如 C 语言里：

```c
int a = 10;
```

是合法语句。

但是：

```c
int = a 10;
```

不是合法语句。

因此，编译器需要一套规则来判断：

```text
哪些字符串属于这门语言？
哪些字符串不属于这门语言？
```

这套规则就是**文法**。

------

# 二、形式语言的几个基本概念

## 1. 字母表 Alphabet

字母表通常记作：

```text
Σ
```

它表示一门语言中允许出现的基本符号集合。

比如二进制语言的字母表是：

```text
Σ = {0, 1}
```

算术表达式的字母表可以粗略看成：

```text
Σ = {0, 1, 2, ..., 9, +, -, *, /, (, )}
```

编程语言的字母表会更复杂，包括：

```text
字母、数字、关键字、运算符、界符等
```

例如：

```text
int, if, else, while, +, -, *, /, (, ), {, }, ;
```

不过严格来说，在编译器的不同阶段，“基本符号”的层次不一样：

在**词法分析**阶段，基本符号可能是字符：

```text
i n t a = 1 0 ;
```

在**语法分析**阶段，基本符号通常是 token：

```text
int   id   =   num   ;
```

------

## 2. 字符串 String

字符串就是由字母表中的符号组成的有限序列。

比如对于：

```text
Σ = {0, 1}
```

那么下面都是字符串：

```text
0
1
01
101
111000
```

还有一个特殊的字符串叫做**空串**，记作：

```text
ε
```

它表示什么符号都没有的字符串。

注意，空串不是空格。空串是长度为 0 的字符串。

------

## 3. Σ* 是什么？

如果：

```text
Σ = {0, 1}
```

那么：

```text
Σ*
```

表示由 0 和 1 能组成的所有有限字符串集合。

也就是：

```text
Σ* = {ε, 0, 1, 00, 01, 10, 11, 000, 001, ...}
```

所以：

> Σ* 表示字母表 Σ 上所有可能字符串的集合。

------

## 4. 语言 Language

语言通常记作：

```text
L
```

它是：

```text
Σ* 的一个子集
```

也就是说，语言是所有可能字符串中的一部分。

例如：

```text
Σ = {0, 1}
```

我们可以定义一门语言：

```text
L = 所有以 1 开头的二进制字符串
```

那么：

```text
1
10
11
101
1000
```

都属于这个语言。

但是：

```text
0
01
001
```

不属于这个语言。

所以可以写成：

```text
L = { w | w 是由 0 和 1 组成，并且 w 以 1 开头 }
```

在编译原理中，语言可以理解为：

> 某种程序设计语言中所有合法程序的集合。

比如：

```text
所有合法的 C 程序 = C 语言
所有合法的 Java 程序 = Java 语言
所有合法的 Go 程序 = Go 语言
```

------

# 三、文法是什么？

文法，英文叫 Grammar。

它是用来描述语言的规则系统。

你可以把文法理解成：

> 一套生成合法字符串的规则。

比如我们想描述简单句子：

```text
小明 吃 苹果
小红 吃 香蕉
小明 喜欢 香蕉
```

我们可以写一套规则：

```text
句子 -> 主语 谓语 宾语
主语 -> 小明 | 小红
谓语 -> 吃 | 喜欢
宾语 -> 苹果 | 香蕉
```

这就是一个简单文法。

通过这套规则，我们可以生成：

```text
小明 吃 苹果
小红 喜欢 香蕉
小明 喜欢 苹果
```

但是生成不了：

```text
吃 小明 苹果
苹果 喜欢 小红
```

因为它们不符合文法规则。

------

# 四、文法的正式定义

一个文法通常表示为：

```text
G = (Vn, Vt, P, S)
```

这四个部分分别是：

```text
Vn：非终结符集合
Vt：终结符集合
P：产生式集合
S：开始符号
```

下面一个个讲。

------

# 五、终结符和非终结符

## 1. 终结符 Terminal

终结符就是最终出现在句子里的符号。

比如在编程语言中：

```text
int
if
else
while
+
*
(
)
id
num
;
```

这些通常是终结符。

比如：

```text
int a = 10;
```

在语法分析阶段可能被看成：

```text
int id = num ;
```

这里：

```text
int、id、=、num、;
```

都是终结符。

终结符的特点是：

> 它不能再继续展开。

------

## 2. 非终结符 Non-terminal

非终结符是语法结构的名字，用来表示某一类结构。

比如：

```text
表达式
语句
函数定义
参数列表
条件语句
循环语句
```

这些都不是代码中直接出现的符号，而是语法结构。

例如：

```text
表达式 -> 表达式 + 项
表达式 -> 项
项 -> 项 * 因子
项 -> 因子
因子 -> num
因子 -> id
```

这里：

```text
表达式、项、因子
```

都是非终结符。

非终结符的特点是：

> 它可以继续被展开。

------

# 六、产生式

产生式就是文法中的规则。

一般写成：

```text
A -> α
```

意思是：

> 非终结符 A 可以替换成 α。

例如：

```text
表达式 -> 表达式 + 项
表达式 -> 项
项 -> 项 * 因子
项 -> 因子
因子 -> id
因子 -> num
```

这些都是产生式。

其中：

```text
- 左边通常是非终结符
- 右边可以是终结符、非终结符，或者它们的组合
```

例如：

```text
语句 -> if ( 表达式 ) 语句
```

这表示一个 if 语句可以由这些部分组成：

```text
if
(
表达式
)
语句
```

其中 `if`、`(`、`)` 是终结符，`表达式` 和 `语句` 是非终结符。

------

# 七、开始符号

开始符号是整个推导开始的地方。

比如我们定义表达式文法：

```text
E -> E + T
E -> T
T -> T * F
T -> F
F -> ( E )
F -> id
```

这里通常把：

```text
E
```

作为开始符号。

意思是：

> 一个完整的表达式从 E 开始生成。

如果是程序语言，开始符号可能是：

```text
Program
```

例如：

```text
Program -> FunctionList
FunctionList -> Function FunctionList
FunctionList -> ε
```

表示一个程序由若干函数组成。

------

# 八、一个完整文法例子

我们用经典的算术表达式文法：

```text
E -> E + T | T
T -> T * F | F
F -> ( E ) | id
```

这里：

```text
E 表示表达式 Expression
T 表示项 Term
F 表示因子 Factor
id 表示变量名或数字
```

这个文法可以生成：

```text
id
id + id
id * id
id + id * id
(id + id) * id
```

它不能生成：

```text
+ id
id *
id + * id
```

因为这些不符合表达式规则。

------

# 九、推导 Derivation

推导就是根据文法规则，一步一步把开始符号变成具体字符串。

比如我们要推导：

```text
id + id * id
```

使用文法：

```text
E -> E + T
E -> T
T -> T * F
T -> F
F -> id
```

推导过程可以是：

```text
E
=> E + T
=> T + T
=> F + T
=> id + T
=> id + T * F
=> id + F * F
=> id + id * F
=> id + id * id
```

最终得到：

```text
id + id * id
```

所以它是这个文法生成的合法句子。

------

# 十、句型和句子

在推导过程中，中间出现的东西叫**句型**。

比如：

```text
E
E + T
T + T
F + T
id + T
id + T * F
id + F * F
id + id * F
id + id * id
```

这些都是句型。

其中：

```text
id + id * id
```

只包含终结符，没有非终结符，所以它叫**句子**。

简单说：

```text
句型：推导过程中的中间形式，可以有非终结符
句子：最终形式，只包含终结符
```

------

# 十一、语言和文法的关系

一个文法 G 能生成很多句子。

这些句子的集合，就是这个文法定义的语言，记作：

```text
L(G)
```

也就是说：

```text
L(G) = 文法 G 能生成的所有句子的集合
```

比如：

```text
G 是算术表达式文法
```

那么：

```text
L(G)
```

就是所有合法算术表达式的集合。

所以关系是：

```text
文法 G 生成语言 L(G)
```

或者说：

```text
语言由文法描述
```

------

# 十二、语法树 Parse Tree

推导过程可以画成树，这棵树叫语法树，也叫分析树。

还是这个表达式：

```text
id + id * id
```

使用文法：

```text
E -> E + T
E -> T
T -> T * F
T -> F
F -> id
```

语法结构大概是：

```text
        E
      / | \
     E  +  T
     |    / | \
     T   T  *  F
     |   |     |
     F   F     id
     |   |
     id  id
```

这棵树表达了一个很重要的信息：

```text
id + id * id
```

应该理解成：

```text
id + (id * id)
```

而不是：

```text
(id + id) * id
```

这说明文法可以体现运算符优先级。

在这个文法里：

```text
E 处理 +
T 处理 *
F 处理 id 或括号
```

由于 `*` 在更深层的 `T` 中处理，所以 `*` 的优先级高于 `+`。

------

# 十三、二义性文法

如果一个文法对同一个句子可以生成两棵不同的语法树，那么这个文法就是**二义性文法**。

比如下面这个文法：

```text
E -> E + E
E -> E * E
E -> ( E )
E -> id
```

它看起来很简单，但是有问题。

对于：

```text
id + id * id
```

它既可以理解成：

```text
id + (id * id)
```

也可以理解成：

```text
(id + id) * id
```

这就产生了二义性。

原因是这个文法没有规定：

```text
* 的优先级高于 +
```

也没有规定：

```text
+ 和 * 的结合方向
```

所以编译器会困惑。

为了解决这个问题，我们通常改写成：

```text
E -> E + T | T
T -> T * F | F
F -> ( E ) | id
```

这样就明确了：

```text
* 优先级高于 +
```

------

# 十四、结合性

结合性指的是多个相同优先级运算符连续出现时，应该从哪边开始结合。

例如：

```text
a - b - c
```

通常减法是左结合，所以应该理解成：

```text
(a - b) - c
```

而不是：

```text
a - (b - c)
```

文法可以体现结合性。

比如左递归：

```text
E -> E - T | T
```

倾向于表达左结合。

推导结构类似：

```text
E
=> E - T
=> E - T - T
=> T - T - T
```

最终结构表示：

```text
(a - b) - c
```

如果写成右递归：

```text
E -> T - E | T
```

则更像右结合：

```text
a - (b - c)
```

所以文法不仅仅判断“是否合法”，还会影响“如何理解”。

------

# 十五、空串 ε

有些结构可以没有内容，这时候会用到空串：

```text
ε
```

比如参数列表可以为空：

```text
ParamList -> Param MoreParams | ε
```

这表示函数可以有参数：

```text
func add(a, b)
```

也可以没有参数：

```text
func main()
```

再比如语句列表：

```text
StmtList -> Stmt StmtList | ε
```

表示一个代码块里可以有很多语句，也可以没有语句。

例如：

```c
{
}
```

也是合法代码块。

------

# 十六、递归文法

很多语言结构天然是递归的。

比如括号表达式：

```text
F -> ( E ) | id
```

因为 `E` 里面又可以包含 `F`，`F` 里面又可以包含 `(E)`。

所以可以生成：

```text
id
(id)
((id))
(((id)))
```

再比如嵌套 if：

```text
Stmt -> if ( Expr ) Stmt
Stmt -> other
```

可以生成：

```c
if (a)
    if (b)
        other
```

编程语言中很多结构都需要递归文法来描述。

------

# 十七、文法分类：乔姆斯基层次

编译原理中常见文法分类来自 Chomsky hierarchy，通常分为四类。

## 1. 0 型文法：无限制文法

最强大，规则限制最少。

对应的语言叫：

```text
递归可枚举语言
```

理论上很强，但在普通编译器前端中不常直接使用。

------

## 2. 1 型文法：上下文有关文法

英文：

```text
Context-Sensitive Grammar
```

它的产生式需要考虑上下文。

比如某个符号能不能替换，取决于它左右两边是什么。

上下文有关文法表达能力很强，但分析复杂度较高，所以编译器中不常作为主要语法分析工具。

------

## 3. 2 型文法：上下文无关文法

英文：

```text
Context-Free Grammar，CFG
```

这是编译原理里最重要的一类。

它的产生式形式通常是：

```text
A -> α
```

左边只能是一个非终结符。

例如：

```text
E -> E + T
T -> T * F
F -> id
```

左边都是单个非终结符。

上下文无关的意思是：

> 不管 A 出现在什么上下文里，它都可以按照 A -> α 来替换。

大多数程序设计语言的语法结构，主要用上下文无关文法描述。

例如：

```text
表达式
语句
循环
条件
函数
代码块
```

这些都适合用 CFG 描述。

------

## 4. 3 型文法：正则文法

正则文法对应正则语言。

它的表达能力比上下文无关文法弱，但分析效率高。

词法分析通常使用正则表达式，也就是正则语言。

例如标识符：

```text
letter (letter | digit)*
```

表示：

```text
以字母开头，后面可以跟字母或数字
```

例如：

```text
abc
a1
userName
```

合法。

但是：

```text
1abc
```

不合法。

所以：

```text
词法分析常用正则文法 / 正则表达式
语法分析常用上下文无关文法
```

这是非常重要的一句话。

------

# 十八、编译器中语言和文法怎么用？

编译器前端大致有几个阶段：

```text
源代码
  ↓
词法分析
  ↓
语法分析
  ↓
语义分析
  ↓
中间代码生成
```

语言和文法主要出现在前两个阶段。

------

## 1. 词法分析：字符变 token

例如源代码：

```c
int a = 10 + 20;
```

词法分析器会把它切成 token：

```text
int     关键字
a       标识符
=       赋值符号
10      数字
+       加号
20      数字
;       分号
```

也可以简化成：

```text
int id = num + num ;
```

词法分析通常用正则表达式描述。

比如：

```text
id  -> letter (letter | digit)*
num -> digit digit*
```

这属于正则语言范畴。

------

## 2. 语法分析：token 变语法树

语法分析器拿到 token 序列：

```text
int id = num + num ;
```

然后根据语法规则判断它是不是合法语句。

比如：

```text
DeclStmt -> Type id = Expr ;
Type -> int | float | char
Expr -> Expr + Term | Term
Term -> num | id
```

然后构造出语法树。

语法分析通常使用上下文无关文法。

------

# 十九、一个完整的小语言例子

假设我们设计一门很小的语言，只支持变量声明和表达式。

例如合法代码：

```text
int a = 1 + 2 * 3;
```

我们可以定义文法：

```text
Stmt -> Type id = Expr ;
Type -> int | float
Expr -> Expr + Term | Term
Term -> Term * Factor | Factor
Factor -> id | num | ( Expr )
```

现在推导：

```text
int id = num + num * num ;
```

过程如下：

```text
Stmt
=> Type id = Expr ;
=> int id = Expr ;
=> int id = Expr + Term ;
=> int id = Term + Term ;
=> int id = Factor + Term ;
=> int id = num + Term ;
=> int id = num + Term * Factor ;
=> int id = num + Factor * Factor ;
=> int id = num + num * num ;
```

这说明：

```text
int a = 1 + 2 * 3;
```

是合法语句。

------

# 二十、文法和语义的区别

文法只能判断结构是否合法，但不能判断所有语义是否正确。

例如：

```c
int a = "hello";
```

从语法上看，它可能符合：

```text
Type id = Expr ;
```

但是语义上有问题，因为：

```text
int 类型不能直接赋值字符串
```

再比如：

```c
int a = b + 1;
```

语法上没问题。

但如果 `b` 没有定义，语义上就有问题。

所以：

```text
语法错误：结构不符合文法
语义错误：结构符合文法，但含义不合法
```

例如：

```c
int = a 10;
```

这是语法错误。

```c
int a = "hello";
```

这是语义错误。

------

# 二十一、BNF 和 EBNF

在编译原理教材里，文法经常用 BNF 或 EBNF 表示。

## 1. BNF

BNF 是 Backus-Naur Form。

例如：

```text
<expr> ::= <expr> + <term> | <term>
<term> ::= <term> * <factor> | <factor>
<factor> ::= id | num | ( <expr> )
```

这里：

```text
::=
```

表示“定义为”。

和：

```text
->
```

差不多。

------

## 2. EBNF

EBNF 是扩展 BNF，写起来更简洁。

常见符号：

```text
{ ... } 表示重复 0 次或多次
[ ... ] 表示可选
( ... ) 表示分组
| 表示或者
```

比如 BNF：

```text
ParamList -> Param MoreParams
MoreParams -> , Param MoreParams | ε
```

可以用 EBNF 写成：

```text
ParamList -> Param { , Param }
```

更直观。

再比如：

```text
if 语句 -> if ( Expr ) Stmt [ else Stmt ]
```

表示 else 部分可有可无。

------

# 二十二、左递归

左递归是编译原理里很重要的概念。

如果一个非终结符可以推导出以自己开头的形式，就叫左递归。

例如：

```text
E -> E + T
```

这就是直接左递归。

因为 E 的右边第一个符号还是 E。

左递归对某些语法分析方法不友好，尤其是 LL 分析，比如递归下降分析。

为什么？

如果直接用递归下降写：

```text
parseE() {
    parseE();
    match("+");
    parseT();
}
```

它会无限调用自己，导致死循环。

所以 LL 分析通常需要消除左递归。

------

## 消除左递归

原文法：

```text
E -> E + T | T
```

可以改写成：

```text
E -> T E'
E' -> + T E' | ε
```

含义是：

```text
表达式 E 先是一个 T，后面可以跟 0 个或多个 + T
```

也就是：

```text
T
T + T
T + T + T
T + T + T + T
```

这和原来的表达能力类似，但不再左递归。

------

# 二十三、左因子提取

有时候几个产生式开头相同，会让分析器不知道选哪条规则。

例如：

```text
Stmt -> if ( Expr ) Stmt
Stmt -> if ( Expr ) Stmt else Stmt
```

两条规则都以：

```text
if ( Expr ) Stmt
```

开头。

对于 LL 分析器来说，看到 `if` 时不知道该选哪一条。

可以提取左因子：

```text
Stmt -> if ( Expr ) Stmt ElsePart
ElsePart -> else Stmt | ε
```

这样就更适合预测分析。

------

# 二十四、FIRST 集和 FOLLOW 集简单理解

如果你继续学语法分析，会遇到 FIRST 集和 FOLLOW 集。

## 1. FIRST 集

FIRST(A) 表示：

> 从 A 推导出的句子，可能以哪些终结符开头。

例如：

```text
Type -> int | float
```

那么：

```text
FIRST(Type) = { int, float }
```

再比如：

```text
Factor -> id | num | ( Expr )
```

那么：

```text
FIRST(Factor) = { id, num, ( }
```

FIRST 集主要用来判断：

```text
看到当前 token 时，应该选择哪条产生式。
```

------

## 2. FOLLOW 集

FOLLOW(A) 表示：

> 在某些句型中，A 后面可能跟哪些终结符。

例如：

```text
Expr -> Expr + Term
Factor -> ( Expr )
```

那么 Expr 后面可能出现：

```text
+
)
;
```

FOLLOW 集通常用于 LL(1) 分析表构造，也用于判断空串产生式什么时候可以使用。

你可以先直觉理解为：

```text
FIRST 看前面能以什么开始
FOLLOW 看后面可能跟着什么
```

------

# 二十五、LL 和 LR 简单理解

文法不是写完就结束了，编译器还要根据文法做语法分析。

常见语法分析方法包括：

```text
LL 分析
LR 分析
```

## 1. LL 分析

LL 的意思是：

```text
Left to right scan
Leftmost derivation
```

也就是：

```text
从左到右读输入，构造最左推导
```

递归下降分析就是一种常见的 LL 思想。

LL 分析的优点：

```text
容易理解
容易手写
适合教学和简单语言
```

缺点：

```text
对文法要求较严格
不能直接处理左递归
```

------

## 2. LR 分析

LR 的意思是：

```text
Left to right scan
Rightmost derivation in reverse
```

也就是：

```text
从左到右读输入，反向构造最右推导
```

LR 分析更强大，很多工业级编译器工具会用 LR 系列方法。

常见的有：

```text
LR(0)
SLR(1)
LR(1)
LALR(1)
```

比如 Yacc、Bison 常使用 LALR(1) 思想。

简单记：

```text
LL：自顶向下分析
LR：自底向上分析
```

------

# 二十六、正则文法和上下文无关文法的区别

这是考试经常考的点。

## 正则文法

适合描述比较简单的线性结构。

例如：

```text
标识符
数字
关键字
注释
字符串字面量的一部分
```

比如标识符：

```text
letter (letter | digit)*
```

可以用正则表达式描述。

------

## 上下文无关文法

适合描述嵌套结构。

例如：

```text
括号匹配
表达式嵌套
if-else 嵌套
函数嵌套
代码块嵌套
```

比如：

```text
(1 + (2 * 3))
```

这种括号嵌套，用正则表达式很难真正描述，但用上下文无关文法很自然：

```text
F -> ( E ) | id
```

所以：

```text
词法分析：正则语言
语法分析：上下文无关语言
```

------

# 二十七、为什么编程语言不能只用正则表达式？

因为编程语言有很多嵌套结构。

例如：

```c
if (a) {
    while (b) {
        if (c) {
            x = 1;
        }
    }
}
```

这里 `{` 和 `}` 必须匹配。

嵌套层数理论上没有固定上限。

正则表达式无法真正处理无限层嵌套结构，而上下文无关文法可以。

比如代码块：

```text
Block -> { StmtList }
StmtList -> Stmt StmtList | ε
Stmt -> Block | OtherStmt
```

这样就可以递归生成任意层嵌套代码块。

------

# 二十八、编译原理中常见考试点

你可以重点掌握这些：

## 1. 会写文法

比如给你语言描述：

```text
由若干个 a 后面跟若干个 b 组成，并且 a 和 b 数量相同
```

语言是：

```text
L = { a^n b^n | n >= 0 }
```

文法可以写成：

```text
S -> a S b | ε
```

推导：

```text
S
=> a S b
=> a a S b b
=> a a ε b b
=> aabb
```

这个文法可以生成：

```text
ε
ab
aabb
aaabbb
```

但不能生成：

```text
abb
aab
abab
```

------

## 2. 会判断字符串是否属于文法语言

给文法：

```text
S -> a S b | ε
```

判断：

```text
aabb
```

是否属于语言。

推导：

```text
S
=> a S b
=> a a S b b
=> a a ε b b
=> aabb
```

所以属于。

判断：

```text
abab
```

不属于，因为这个文法只能生成所有 a 在前、所有 b 在后的字符串。

------

## 3. 会画语法树

对表达式：

```text
id + id * id
```

用文法：

```text
E -> E + T | T
T -> T * F | F
F -> id
```

画出的结构应该体现：

```text
id + (id * id)
```

------

## 4. 会判断文法是否二义性

如果一个句子有两种不同语法树，文法就是二义性的。

经典例子：

```text
E -> E + E | E * E | id
```

对：

```text
id + id * id
```

有两种理解，因此二义。

------

## 5. 会消除左递归

例如：

```text
E -> E + T | T
```

改成：

```text
E -> T E'
E' -> + T E' | ε
```

------

## 6. 会提取左因子

例如：

```text
A -> α β1 | α β2
```

改成：

```text
A -> α A'
A' -> β1 | β2
```

具体例子：

```text
Stmt -> if Expr then Stmt
Stmt -> if Expr then Stmt else Stmt
```

可改为：

```text
Stmt -> if Expr then Stmt ElsePart
ElsePart -> else Stmt | ε
```

------

# 二十九、一个适合你记忆的总图

你可以这样理解：

```text
字符
  ↓ 词法规则，通常是正则表达式
Token
  ↓ 语法规则，通常是上下文无关文法
语法树 / AST
  ↓ 语义分析
带类型和作用域信息的语法树
  ↓ 中间代码生成
IR
  ↓ 优化
优化后的 IR
  ↓ 目标代码生成
机器码 / 汇编
```

其中：

```text
语言和文法主要服务于词法分析和语法分析。
```

------

# 三十、最核心总结

语言是：

```text
某个字母表上所有合法字符串的集合。
```

文法是：

```text
用来生成或描述这些合法字符串的规则系统。
```

文法一般写成：

```text
G = (Vn, Vt, P, S)
```

其中：

```text
Vn：非终结符
Vt：终结符
P：产生式
S：开始符号
```

编译器里：

```text
正则文法 / 正则表达式
主要用于词法分析

上下文无关文法 CFG
主要用于语法分析
```

一句话背下来：

> **词法分析把字符变成 token，语法分析根据文法把 token 组织成语法树。**

再进一步：

> **文法不仅判断代码是否合法，还决定代码的结构、优先级、结合性和语法树形态。**